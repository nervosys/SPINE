[package]
name = "spine-embedded"
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
version = "0.2.0"
readme = "README.md"
edition.workspace = true
license.workspace = true
description = "Minimal agent runtime for embedded/IoT targets (ARM Cortex-M, ESP32, RISC-V)"

[dependencies]
spine-nostd = { path = "../spine-nostd", version = "2.0.0" }

[features]
default = []
alloc = ["spine-nostd/alloc"]
