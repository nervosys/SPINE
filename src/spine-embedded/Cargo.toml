[package]
name = "spine-embedded"
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Minimal agent runtime for embedded/IoT targets (ARM Cortex-M, ESP32, RISC-V)"

[dependencies]
spine-nostd = { path = "../spine-nostd", version = "1.0.0" }

[features]
default = []
alloc = ["spine-nostd/alloc"]
